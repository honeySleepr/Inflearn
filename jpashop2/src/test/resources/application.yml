#spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

# Spring Boot는 메모리 기반 h2가 default이기 때문에 따로 설정해주지 않아도 된다.

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
